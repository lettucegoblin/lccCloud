{
  "name": "lcccloud",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "cd server && node index.js",
    "client_install": "cd lcccloud-front && npm install",
    "build_client_win": "cd lcccloud-front && npm run build && xcopy dist ..\\server\\dist /E /Y",
    "build_client": "cd lcccloud-front && npm run build && cp -r dist ../server/dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "node-pty": "^1.0.0",
    "socket.io": "^4.7.5"
  }
}
